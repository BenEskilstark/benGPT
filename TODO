
  - bugs
    - new conversation when you have a name overlap + x will clear convo

  - show something while waiting for response
  - way to X out of import modal
  - way to bring back apiKey modal
  - set thread params
  - submit/clear/undo should stack on top of each other when in big text mode

  - npm package
    - index.js with Chat, Thread, and gpt.js functions
    - reducer for tracking conversations
